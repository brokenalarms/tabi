name: Vimium
options:
  bundleIdPrefix: com.brokenalarms
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "16.3"

settings:
  base:
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    DEVELOPMENT_TEAM: F73P768296

schemes:
  Vimium:
    build:
      targets:
        Vimium: all
        Vimium Extension: all
    run:
      executable: Vimium

targets:
  Vimium:
    type: application
    platform: macOS
    sources:
      - path: Vimium/Host App
      - path: Vimium/Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.brokenalarms.Vimium
        CODE_SIGN_ENTITLEMENTS: Vimium/Host App/Vimium.entitlements
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_KEY_CFBundleDisplayName: Vimium
        GENERATE_INFOPLIST_FILE: true
        ALWAYS_EMBED_SWIFT_STANDARD_LIBRARIES: true
    dependencies:
      - target: Vimium Extension
        embed: true
        codeSign: true
        copy:
          destination: plugins
          subpath: ""

  Vimium Extension:
    type: app-extension
    platform: macOS
    sources:
      - path: Vimium/Safari Extension
        excludes:
          - "Resources/**"
      - path: Vimium/Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.brokenalarms.Vimium.Extension
        CODE_SIGN_ENTITLEMENTS: Vimium/Safari Extension/VimiumExtension.entitlements
        CODE_SIGN_STYLE: Automatic
        GENERATE_INFOPLIST_FILE: true
        INFOPLIST_FILE: Vimium/Safari Extension/Info.plist
        OTHER_LDFLAGS: ["-framework", "SafariServices"]
    preBuildScripts:
      - name: Compile TypeScript
        basedOnDependencyAnalysis: false
        script: |
          cd "$PROJECT_DIR"
          export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
          npm run build
    postBuildScripts:
      - name: Copy Extension Resources
        script: |
          rsync -a --delete "${PROJECT_DIR}/Vimium/Safari Extension/Resources/" "${BUILT_PRODUCTS_DIR}/${UNLOCALIZED_RESOURCES_FOLDER_PATH}/"
        inputFiles:
          - $(PROJECT_DIR)/Vimium/Safari Extension/Resources/
        outputFiles:
          - $(BUILT_PRODUCTS_DIR)/$(UNLOCALIZED_RESOURCES_FOLDER_PATH)/manifest.json
      - name: Validate Extension Bundle
        script: |
          PLIST="${BUILT_PRODUCTS_DIR}/${INFOPLIST_PATH}"
          # NSExtension dictionary is required for Safari to recognize the extension
          if ! /usr/libexec/PlistBuddy -c "Print :NSExtension:NSExtensionPointIdentifier" "$PLIST" &>/dev/null; then
            echo "error: NSExtension dictionary missing from extension Info.plist. Safari will silently ignore this extension."
            echo "error: Ensure Vimium/Safari Extension/Info.plist exists and contains the NSExtension dictionary."
            exit 1
          fi
          # Verify manifest.json was copied
          RESOURCES_DIR="${BUILT_PRODUCTS_DIR}/${UNLOCALIZED_RESOURCES_FOLDER_PATH}"
          if [ ! -f "$RESOURCES_DIR/manifest.json" ]; then
            echo "error: manifest.json missing from extension bundle Resources. The extension will not load in Safari."
            exit 1
          fi
          echo "Extension bundle validation passed."
          touch "${DERIVED_FILE_DIR}/validate_extension.stamp"
        inputFiles:
          - $(BUILT_PRODUCTS_DIR)/$(INFOPLIST_PATH)
          - $(BUILT_PRODUCTS_DIR)/$(UNLOCALIZED_RESOURCES_FOLDER_PATH)/manifest.json
        outputFiles:
          - $(DERIVED_FILE_DIR)/validate_extension.stamp
